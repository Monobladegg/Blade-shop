import { ICategory } from 'src/types/db'

interface Props {
  db: ICategory[],
  category: number,
  active: number,
}

export const Card = (PropsTemporarily: Props) => {
  return (
    <div>

    </div>
  )
}